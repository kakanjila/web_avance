FROM php:8.2-apache

# Required by pdo_pgsql build step (provides libpq-fe.h and pg_config)
RUN apt-get update \
	&& apt-get install -y --no-install-recommends libpq-dev \
	&& rm -rf /var/lib/apt/lists/*

RUN docker-php-ext-install pdo pdo_pgsql
RUN a2enmod rewrite
